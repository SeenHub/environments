#!/usr/bin/env bash

# Based on Ubuntu server 11.10

if [ "$(whoami)" != 'deploy' ]
  then
    echo 'You must run this as deploy.'
    exit 0
fi

ssh-keygen -t rsa

echo "Now add relevant public keys to /home/deploy/.ssh/authorized_keys"
